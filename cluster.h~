#pragma once
#include <queue>
using std::queue;
#include "job.h"

class cluster {
public:
  unsigned int num_processors;
  unsigned int* processors;
  queue<job> job_queue;
  cluster(unsigned int);
  void add_job(job);
  void run_scheduler();
  ~cluster();
};
